// Barrel for the animated icon set — import icons from here exactly as you would
// from "@/components/icons" (same names + props). See AnimatedIcon.tsx / icons.css.
export * from "./AnimatedIcon";
export type { AnimatedIconProps } from "./AnimatedIcon";
